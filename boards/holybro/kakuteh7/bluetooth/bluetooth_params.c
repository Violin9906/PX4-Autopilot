/**
 * Bluetooth mode
 *
 * 0: always off
 * 1: always on
 * 2: on when disarmed, off when armed
 *
 * @value 0 Off
 * @value 1 On
 * @value 2 Auto
 * @group Board
 */
PARAM_DEFINE_INT32(BT_MODE, 0);
